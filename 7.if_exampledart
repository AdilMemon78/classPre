//write a program cheak grethen number

import 'dart:io';

main()
{
    var num;

    print("Enter a number :");
    num=int.parse(stdin.readLineSync()!);

    if(num>35)
    {
        print('number is ${num} is biggest number then 35');
    }
}